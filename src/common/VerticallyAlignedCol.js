import styled from 'styled-components';
import Col from 'react-bootstrap/Col';

const VerticallyAlignedCol = styled(Col)`
  display: flex;
  flex-direction: column;
  justify-content: center;
`;

export default VerticallyAlignedCol;
